<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:dither="true"
    android:shape="oval"
    android:useLevel="false" >

    <!-- android:shape="oval"表示所要绘制的图形是一个椭圆，默认是rectangle，长方tab_msgnum_textbg.xmltab_msgnum_textbg.xml形 -->
    <gradient
        android:angle="270"
        android:centerColor="#E70F19"
        android:centerY="0.75"
        android:endColor="#C50103"
        android:startColor="#F8B3B7" />
    <!-- gradient 产生颜色渐变 android:angle 从哪个角度开始变 只有90的整数倍可以 -->
    <solid android:color="#C50103" />
    <!-- solid表示图形是实心的，填充里面，#ff4100ff为填充颜色 -->
   <!-- <stroke
        android:width="2dp"
        android:color="#E3E3E3" />-->
    <!-- 描边 采用那样的方式将外形轮廓线画出来，width表示笔的粗细，dashWidth表示小横线的宽度，dashGap表示小横线之间的距离 -->
    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp" />
    <!-- 和CSS中的padding应该是一个道理 -->
    <corners android:radius="6dp" />
    <!-- corners表示是有半径为5像素的圆角 -->

</shape>